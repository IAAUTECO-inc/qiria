syntax = "proto3";

package qiria;

option go_package = "qiria/core/api/gen";

// 🇬🇧 The core service definition for Qiria.
// 🇫🇷 La définition du service principal de Qiria.
service QiriaCore {
  // 🇬🇧 Requests a report to be generated. This is an asynchronous operation.
  // 🇫🇷 Demande la génération d'un rapport. C'est une opération asynchrone.
  rpc RequestReport (ReportRequest) returns (ReportResponse);

  // 🇬🇧 Retrieves the status and result of a previously requested report.
  // 🇫🇷 Récupère le statut et le résultat d'un rapport précédemment demandé.
  rpc GetReportResult (ReportResultRequest) returns (ReportResultResponse);
}

// 🇬🇧 The request message for creating a report.
// 🇫🇷 Le message de requête pour la création d'un rapport.
message ReportRequest {
  string report_id = 1;
  string parameters_json = 2; // Parameters for the report in JSON format.
}

// 🇬🇧 The response message containing the task ID for the report generation.
// 🇫🇷 Le message de réponse contenant l'ID de la tâche pour la génération du rapport.
message ReportResponse {
  string task_id = 1;
  string status = 2; // e.g., "QUEUED", "INVALID_REQUEST"
}

message ReportResultRequest {
  string task_id = 1;
}

message ReportResultResponse {
  string task_id = 1;
  string status = 2; // e.g., "PENDING", "RUNNING", "COMPLETED", "FAILED"
  string result_json = 3; // The actual report data, in JSON format.
}